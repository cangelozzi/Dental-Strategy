<?php

namespace Tests\Unit;

use Tests\TestCase;
use Illuminate\Foundation\Testing\WithFaker;
use Illuminate\Foundation\Testing\RefreshDatabase;

use App;

class LessonTest extends TestCase
{
    // /** @test */
    // function it_should_have_pages()
    // {
    //     $page = (App\Lesson::find(1)->pages()->get())[0];
    //     $this->assertTrue(get_class($page) == App\Page::class);
    // }

    // /** @test */
    // function it_should_have_tags_through_modules()
    // {
    //     $module = (App\Lesson::find(1)->module()->get())[0];
    //     $tag = ($module::find(1)->tags()->get())[0];

    //     $this->assertTrue(get_class($tag) == App\Tag::class);
    // }
}
